# CLAUDE.md — Hướng dẫn cho Claude Code

Tài liệu này dành riêng cho Claude Code khi làm việc trên repo **AWOG**. Đọc trước khi sửa code.

## Tóm tắt dự án

AWOG (**Artifact Workflow Orchestrate Guild**) là một AI Team Operating System **local-first**, đóng gói thành desktop app qua **Electron**, với Nuxt 4 làm UI + engine (Node.js). Xem [README.md](README.md) và [artifacts/VISION.md](artifacts/VISION.md) để nắm tầm nhìn.

## Bản đồ tài liệu (đọc theo thứ tự khi onboard)

1. [artifacts/VISION.md](artifacts/VISION.md) — tầm nhìn sản phẩm
2. [docs/requirements/product-overview.md](docs/requirements/product-overview.md) + [docs/requirements/mvp-scope.md](docs/requirements/mvp-scope.md) — phạm vi MVP
3. [docs/architecture/system-overview.md](docs/architecture/system-overview.md) — kiến trúc tổng quan + sơ đồ
4. [docs/architecture/data-model.md](docs/architecture/data-model.md), [docs/architecture/execution-model.md](docs/architecture/execution-model.md)
5. [docs/architecture/tech-stack.md](docs/architecture/tech-stack.md) — stack chi tiết
6. [docs/decisions/](docs/decisions/) — ADR (đọc khi cần hiểu *vì sao*)
7. [apps/desktop/ui-next/](apps/desktop/ui-next/) — Nuxt UI hiện tại (SPA rebuild theo prototype)

## Quy ước quan trọng

- **Ngôn ngữ tài liệu: tiếng Việt.** Toàn bộ `docs/`, README, comment kỹ thuật → tiếng Việt. Code, identifier, log message → tiếng Anh.
- **Coding style:** đọc [docs/coding/general.md](docs/coding/general.md) (cross-stack) + [docs/coding/nuxt-frontend.md](docs/coding/nuxt-frontend.md) (UI). Đừng tự ý thay đổi convention; nếu cần thay đổi, mở thảo luận trước.

## Quick-scan rules (.claude/rules/)

Trước khi sinh code, scan file rule tương ứng:

@.claude/rules/principles.md
@.claude/rules/typescript.md
@.claude/rules/nuxt-vue.md
@.claude/rules/lint-format.md
@.claude/rules/git-commit.md
@.claude/rules/security.md

## Role-based agents (.claude/agents/)

Mỗi giai đoạn delivery có 1 agent chuyên trách + skill workflow đi kèm:

| Agent | Vai trò | Skill |
|---|---|---|
| `product-owner` | Định hướng, đánh giá feature idea vs VISION | `write-feature-brief` |
| `business-analyst` | Spec chi tiết, AC, edge case | `elicit-requirements` |
| `project-manager` | Chia task, ước lượng, dependency | `decompose-tasks` |
| `tech-lead` | Quyết định kiến trúc, ADR | `write-adr` |
| `developer` | Implement task, lint/typecheck | `implement-feature` |
| `qa-tester` | Test case, verify AC, edge case | `write-test-cases` |
| `code-reviewer` | Review chất lượng/architecture/perf | `review-pr` |
| `infosec` | Audit security, 21 rule + 8 invariant AWOG | `security-audit` |

Pipeline điển hình: **PO → BA → PM → TL → developer → QA → reviewer (+ infosec khi đụng surface)**.
- **Đặt tên file:** `kebab-case.md`, `kebab-case.vue`, `PascalCase.vue` cho component. ADR đánh số `NNNN-title.md`.
- **Liên kết chéo:** dùng relative path trong tài liệu.
- **Một file = một chủ đề.** Đừng nhồi nhiều khái niệm vào một tài liệu.

## Stack hiện tại

| Lớp | Công nghệ | Ghi chú |
|---|---|---|
| Desktop shell | **Electron** (main process = Node) | [ADR 0027](docs/decisions/0027-tauri-vs-electron-revisit.md) thay Tauri ([migration](docs/features/electron-migration.md)). `apps/desktop/electron/`: main (BrowserWindow + tray + single-instance + `app://` protocol), engine spawn qua `child_process.spawn(process.execPath, ELECTRON_RUN_AS_NODE=1)` + **stdio JSON-RPC giữ nguyên**, IPC `contextBridge` → `window.awog` (contextIsolation+sandbox, no nodeIntegration). Đóng gói electron-builder (dmg/nsis/AppImage/deb). **Auto-update** `electron-updater` ([ADR 0028](docs/decisions/0028-auto-update.md), [spec](docs/features/auto-update.md)): GitHub provider, `autoDownload=false` (hỏi trước khi tải), Win+Linux(AppImage) auto-install / mac+`.deb` notify-only; renderer (`stores/update.ts`) drive lịch check, main (`updater.ts`) reactive qua kênh `updater:event`. |
| Frontend | Nuxt 4 (SPA, `ssr: false`) + Vue 3 + TS | Đã port core features. **Agents/Skills/MCP/Projects/Git/Sessions/Tasks/Workflows wired** (Sessions có Workspace Panel: Diff/Files/Browser/Plan/Terminal/Tasks/Preview — [spec](docs/features/workspace-panel.md)). Tasks chạy thật qua engine ([ADR 0024](docs/decisions/0024-task-execution-engine-ipc-contract.md)). |
| State | Pinia | Store: `workspace` (projects/agents/skills/mcp/hooks/commands), `tasks` (live — event-driven execution, app-lifetime subscribe), `workflows` (live — DAG persist + debounce), `settings` (live), `sessions` (live), `git` (live + browser fallback). Tasks/Workflows tách store riêng; browser-dev có mock fallback. |
| Canvas | VueFlow | Workflow DAG editor (live — 2 tier: global `~/.awog/workflows/<id>.json` + per-project `{project}/.awog/workflows/<id>.json`, scope selector + tier badge) |
| Styling | TailwindCSS 3 + inline `:style` theme token | Xem `useTheme()` composable. **Theme family** ([ADR 0072](docs/decisions/0072-cute-theme-family.md), [spec](docs/features/theme-cute.md)): `awog` (mặc định) hoặc **`cute`** (mint/off-white, opt-in ở Settings → Appearance → Theme → Cute) qua `body[data-theme-family]` + `assets/css/theme-cute.css` (nạp cuối `css` array, mọi rule scoped theo attribute — thắng specificity của `<style scoped>` component mà không cần `!important`); `shadcn` còn là placeholder chưa implement. |
| Icons | lucide-vue-next | |
| Code editor | Monaco | Cho prompt + artifact (markdown editor full-screen) + **Project Code Workspace** (`MonacoEditor.vue`, model-cache multi-tab, worker bundle local, [ADR 0021](docs/decisions/0021-monaco-code-editor.md)) |
| Engine (Sidecar) | Node.js `@awog/sidecar` | Wired M7+M8+Pha2A: OAuth, Sessions, Skills, MCP/**Connections** (stdio+http+keychain+per-agent whitelist+idle stop; global-only, danh sách phẳng — trang "MCP Servers" đổi tên thành "Connections" theo [ADR 0025](docs/decisions/0025-connections-manager.md) đã đơn giản hoá, mô hình Craft "Sources"; New Task có dropdown chọn connection tùy chọn → engine union vào mọi node, KHÔNG service enum/tier), Agents (AGENT.md 2-tier trên `.claude`+runtime systemPrompt/tools/skillIds/mcpServerIds injection), Git Manager (24 method), filesystem watcher (chokidar), `fs.*` read-write + search ([ADR 0022](docs/decisions/0022-fs-read-write-search-ipc.md): listDir/readFile/listFiles/writeFile/createFile/createDir/rename/delete/search/watch/unwatch — write/delete/search gated by `assertInsideWorkspace`), `terminal.*` PTY (node-pty, [ADR 0019](docs/decisions/0019-pty-terminal-in-sidecar.md)). **Task Execution Engine** ([ADR 0024](docs/decisions/0024-task-execution-engine-ipc-contract.md)): `workflows.*` (JSON snapshot + DAG validate) + `tasks.*` (JSONL event-sourced, parallel scheduler cap 4, per-node `invokeSdk` reuse Pi core, trace stream, git auto-commit per node, approve/rerun/discuss, restart-safe resume). Use **Pi SDK** (single multi-provider runtime — [ADR 0029](docs/decisions/0029-migrate-llm-runtime-to-pi-sdk.md)). |
| LLM client | **Pi SDK** (`@earendil-works/pi-ai` + `pi-agent-core`) — single runtime | Multi-provider (Anthropic/OpenAI/Google/custom) via `runAgentLoop`; in-process AgentTools + MCP bridge; resume rebuild từ JSONL; permission hook `beforeToolCall`. OAuth + API-key credential flow. **Lõi prompt cấp senior** ([ADR 0071](docs/decisions/0071-senior-engineer-prompt-core.md)): dưới OAuth Pi chỉ prepend 1 câu identity, nên AWOG tự cấp orientation (`<environment>` + `<current_state>`) + ENGINEERING/EVIDENCE/COMMUNICATION + tool description mức chính sách; nhánh Claude SDK đã có preset `claude_code` nên chỉ bù VERIFY + EVIDENCE (hằng ⇒ nằm ở append) và `<current_state>` mỗi turn. Block `OUTPUT_SURFACE_PROMPT` ([ADR 0077](docs/decisions/0077-output-surface-no-hard-wrap.md)) dùng chung **cả 2 runtime** trên đủ 5 điểm append (kể cả subagent): nói cho model biết bề mặt là markdown render trong panel GUI co giãn, một đoạn văn = một dòng, cấm hard-wrap kể cả prose trong fence `markdown` (PR/issue body, commit message) — đây là **đính chính** giả định "output ra terminal" của preset, không phải trùng lặp; kèm việc bỏ câu "Keep all of it legible in a terminal." trong `COMMUNICATION_PROMPT`. **Subagent `Task` tool** ([ADR 0030](docs/decisions/0030-subagent-task-tool.md), [spec](docs/features/subagent-task-tool.md)): delegate sang AWOG agent (honor provider/model/account của AGENT.md, depth=1, nested step/trace qua `parentId`), bật ở Sessions + Tasks, graceful fallback (khử lỗi `Tool Task not found`). **Mức suy luận** ([ADR 0078](docs/decisions/0078-reasoning-effort-parity.md)) map 1:1 đủ 5 bậc trên cả 3 runtime (`extra-high → xhigh`); riêng nhánh Claude SDK có **bậc thứ sáu NGOÀI thang `EffortLevel`** — cờ `Settings.ultracode` ([ADR 0089](docs/decisions/0089-ultracode-effort-tier.md)) = xhigh + điều phối dynamic-workflow, đi ở `Options.settings` cạnh `attribution`, hiện ra là hàng cuối của picker effort trong PHIÊN và **chỉ** với provider `anthropic`. Dữ liệu tách (`SessionSettings.ultracode`, KHÔNG nhét bậc mới vào `ThinkingLevel` dùng chung cho Pi/Codex/4 bề mặt Settings), UI gộp làm một danh sách loại trừ nhau, **runtime là nơi cưỡng chế** (`effortFromSettings`/`thinkingFromSettings` trả xhigh + thinking bật bất kể `level` — không có chỗ này thì phiên cũ gửi `--effort low` cạnh cờ tự nhận là xhigh); chỉ ghi cờ khi BẬT vì `false` sẽ đè `~/.claude/settings.json` của user (`settingSources` có `'user'`). Điều kiện phía CLI (workflows bật + model hỗ trợ xhigh + org không chặn) **không** gate được từ AWOG — thiếu thì CLI *lặng lẽ* bỏ qua, nên nói trước ở tooltip. Kèm theo: node của Task nay chụp `thinkingLevel` lúc tạo (cùng đường `commitCoAuthor`) thay vì ghim cứng `'medium'` — trước đó picker của user không bao giờ tới được Tasks trên bất kỳ runtime nào. Xem [ADR 0029](docs/decisions/0029-migrate-llm-runtime-to-pi-sdk.md). |
| Storage | Filesystem JSON/YAML/MD + Git + JSONL + OS keychain | credentials.json (chmod 600) + sessions JSONL + projects.json + mcp-servers/<id>.json (Connections; global `~/.awog/mcp-servers/`; env/header `secret:KEY` ref → OS keychain via `@napi-rs/keyring`, token KHÔNG bao giờ vào file/git, [ADR 0018](docs/decisions/0018-mcp-secret-keychain.md)) + agents/<id>.md (Claude Code subagent format). **Nhà của config-entity tách theo loại ([ADR 0070](docs/decisions/0070-share-claude-home-for-config.md), supersede một phần [ADR 0035](docs/decisions/0035-consolidate-config-tiers-to-awog.md)):** **skills / agents / commands** dùng chung `.claude` với Claude Code CLI (`~/.claude/{kind}` + `{project}/.claude/{kind}`, honor `CLAUDE_CONFIG_DIR`) → sửa bên nào cũng live ngay bên kia, KHÔNG còn import; **hooks / rules** + toàn bộ data AWOG-only (sessions, credentials, projects, workflows, sources, ssh/vpn, settings, templates) ở `.awog`. Boot migration `migration/claude-home.ts` tự move + xoá thư mục cũ (bản `.claude` thắng khi trùng tên; bản lệch nội dung park vào `~/.awog/migrated-conflicts/`). |
| Git Manager | Wired M0..M7 ([ADR 0017](docs/decisions/0017-git-manager-ipc-contract.md), [spec](docs/features/git-manager.md)) | 24 method `git.*` per-command (bao gồm `stageHunk`, `init`, `discoverRepos`), mutex per workspace, system git ≥ 2.20 với bootstrap probe + banner, chokidar watcher với debounce 200ms, virtual scroll > 200 file/section, detached HEAD warn, NO_REPO empty state + init CTA. **Multi-repo per project**: `discoverRepos` quét folder project (≤2 cấp) tìm repo con → header có repo picker khi project là container nhiều repo (project root không phải repo). **Auto-fetch** background mặc định 5 phút (silent, on git page open + window focus; chỉnh/tắt ở Settings → Workspace). **Branch tree** trong sidebar (gom theo prefix `/`, folder default đóng, current branch accent); **Changes tree/flat toggle** (default tree, persisted). UI Sublime-Merge style: sidebar resizable/collapsible (Local Changes / All Commits / Branches / Remotes / Tags / Stashes / Submodules) + main pane theo selection + progress strip overlay (absolute, không shift layout). I18n en/vi. Store `git` còn fallback mock cho browser dev. |

## Lệnh hay dùng

```bash
# UI dev
cd apps/desktop/ui-next
pnpm install
pnpm dev            # http://localhost:3031
pnpm typecheck      # vue-tsc strict
pnpm lint           # ESLint 9 flat config (@nuxt/eslint + Vue + TS) + Prettier
pnpm lint:fix       # auto-fix lint + format
pnpm format         # Prettier toàn bộ
pnpm build
```

> Repo dùng **pnpm workspaces** ([pnpm-workspace.yaml](pnpm-workspace.yaml)). Khi thêm dependency, chạy trong đúng package.

## File / thư mục quan trọng

| Path | Vai trò |
|---|---|
| [apps/desktop/ui-next/app.vue](apps/desktop/ui-next/app.vue) | Root component |
| [apps/desktop/ui-next/layouts/default.vue](apps/desktop/ui-next/layouts/default.vue) | Shell: NavRail + TopBar |
| [apps/desktop/ui-next/stores/](apps/desktop/ui-next/stores/) | Pinia stores tách theo domain (agents/skills/projects/tasks/workflows/sessions/git/settings…) |
| [apps/desktop/ui-next/stores/settings.ts](apps/desktop/ui-next/stores/settings.ts) | API keys, workspace path, connectors |
| [apps/desktop/sidecar/src/wiki/](apps/desktop/sidecar/src/wiki/) | Wiki store 2-tier (`.awog/wiki`) + `<wiki_index>` inject + grep search/backlink ([ADR 0073](docs/decisions/0073-wiki-as-llm-context-source.md)) |
| [apps/desktop/ui-next/pages/wiki.vue](apps/desktop/ui-next/pages/wiki.vue) | Trang Wiki (cây · reader · outline/backlinks) — state ở `composables/useWikiManager.ts` |
| [apps/desktop/sidecar/src/monitor/](apps/desktop/sidecar/src/monitor/) | Giám sát tài nguyên ([ADR 0090](docs/decisions/0090-activity-monitor-process-attribution.md), [spec](docs/features/activity-monitor.md)): `ps` + hiệu CPU giữa 2 nhịp + quy tiến trình về phiên 3 tầng (sổ đăng ký `owned.ts` → `--resume=<sdkSessionId>` trên argv → thừa kế cha-con) + dò tiến trình mồ côi + cổng kiểm `monitor.kill`. **Tab "Đĩa"**: ổ đĩa + danh mục rác **tự đo ngay khi mở tab** (sắp theo dung lượng giảm dần — để người dùng bấm mới đo thì cột Dung lượng toàn `—`, mà con số đó chính là thứ duy nhất làm nó thành "gợi ý") + quét sâu streaming (`readdir` rồi `du -skx` từng con — `du` KHÔNG stream khi stdout là pipe: đo được 400 dòng về cùng lúc ở giây 21.7). **Ba đường ghi, mỗi cái một cổng**: `monitor.kill` · `shell.trashItem` ở Electron main (xoá = **Thùng rác**, hoàn tác được; ba hàng rào: tuyệt đối + `realpath`, trong nhà nhưng không phải chính thư mục nhà, sâu ≥ 2 cấp) · `cleanup.run` — **danh mục lệnh CỐ ĐỊNH** trong `monitor/cleanup.ts`, UI chỉ gửi `id` nên không có bề mặt command injection; cố ý `docker system prune -f` chứ KHÔNG `-a --volumes` (xoá volume = mất database của user). **Menu chuột phải + nút Hiện-trong-Finder/Sao-chép-đường-dẫn dùng chung `useFileContextMenu`** với tab Files của Sessions qua cờ `absolute` (đường dẫn tuyệt đối, không workspace root ⇒ tự bỏ VS Code/mở-bằng-app-mặc-định/copy-đường-dẫn-tương-đối/`fs.*`, xoá đi qua `onTrash` = Thùng rác). Reveal là IPC RIÊNG `shell:revealDiskPath` chứ KHÔNG nới `shell:revealPath` — cái cũ đòi workspace root nên sẽ chết ở đúng `/Applications`, `/Library`, ổ ngoài; phạm vi mới khớp phần ĐỌC (chặn `/dev`,`/proc`,`/sys`,snapshot), và cố ý KHÔNG có "mở bằng app mặc định" vì nó CHẠY trình xử lý của file. ⚠ Nút Thùng rác ở bảng gợi ý nay cũng gate `canTrash` như cây quét sâu: `projectRoots` có thể trỏ ổ ngoài ⇒ trước đó nút luôn hiện và luôn bị main từ chối |
| [apps/desktop/ui-next/pages/monitor.vue](apps/desktop/ui-next/pages/monitor.vue) | Trang Giám sát `/monitor` — state ở `composables/useMonitorManager.ts`. ⚠ KHÁC `/activity` (token + tiền) |
| [apps/desktop/sidecar/src/memory/](apps/desktop/sidecar/src/memory/) | Bộ nhớ AI: store 1-fact-1-file 2 tier + `<memory>` inject theo ngân sách ([ADR 0073](docs/decisions/0073-wiki-as-llm-context-source.md) phần B) |
| [apps/desktop/ui-next/components/settings/SettingsMemory.vue](apps/desktop/ui-next/components/settings/SettingsMemory.vue) | Settings → Bộ nhớ: công tắc auto-write + danh sách fact (sửa/xoá/toggle/xoá hết) |
| [apps/desktop/ui-next/composables/useTheme.ts](apps/desktop/ui-next/composables/useTheme.ts) | Theme tokens (dark/light) |
| [apps/desktop/ui-next/composables/useThemeFamily.ts](apps/desktop/ui-next/composables/useThemeFamily.ts) | `{ family, isCute }` — chỉ dùng ở nơi theme "Cute" đổi MARKUP, không phải style thuần CSS ([ADR 0072](docs/decisions/0072-cute-theme-family.md)) |
| [apps/desktop/ui-next/assets/css/theme-cute.css](apps/desktop/ui-next/assets/css/theme-cute.css) | Toàn bộ theme "Cute" (mint/off-white) — mọi rule scoped `body[data-theme-family='cute']` ([spec](docs/features/theme-cute.md)) |
| [apps/desktop/ui-next/components/common/AwogMascot.vue](apps/desktop/ui-next/components/common/AwogMascot.vue) | Mascot SVG inline dùng ở theme Cute (logo/empty state) — KHÔNG phải spritesheet desktop-pet |
| [apps/desktop/ui-next/types/index.ts](apps/desktop/ui-next/types/index.ts) | Entity types (Task, Project, Agent, Skill, Workflow) |
| [apps/desktop/sidecar/src/skills/](apps/desktop/sidecar/src/skills/) | Skill storage: 2-tier scan trên `.claude` (ADR 0070) + atomic SKILL.md write + `loadSkillByIdAnyTier` |
| [apps/desktop/sidecar/src/agents/](apps/desktop/sidecar/src/agents/) | Agent storage: 2-tier AGENT.md trên `.claude` (ADR 0070; both single-file + folder/AGENT.md layout) |
| [apps/desktop/sidecar/src/mcp/](apps/desktop/sidecar/src/mcp/) | McpManager (stdio+http+idle stop), HttpMcpClient + SSRF guard, secrets helper, store |
| [apps/desktop/sidecar/src/credentials/keychain.ts](apps/desktop/sidecar/src/credentials/keychain.ts) | OS keychain wrapper qua `@napi-rs/keyring` (dynamic import + graceful fallback) |
| [apps/desktop/sidecar/src/watcher.ts](apps/desktop/sidecar/src/watcher.ts) | Filesystem watcher chokidar — emit `*.fs-changed` events (watch `.claude` cho skills/agents/commands, `.awog` cho phần còn lại) |
| [apps/desktop/sidecar/src/methods/](apps/desktop/sidecar/src/methods/) | 50+ RPC methods: skills.*, agents.*, mcp.*, sessions.*, projects.*, git.*, auth.*, accounts.* |
| [apps/desktop/sidecar/src/context/](apps/desktop/sidecar/src/context/) | Prompt context builder ([ADR 0071](docs/decisions/0071-senior-engineer-prompt-core.md)): `memory-files.ts` (CLAUDE.md/AGENTS.md + `~/.claude/CLAUDE.md`, expand `@import` đệ quy, allowlist 2 root) + `environment.ts` (`<environment>` ổn định cho system prompt / `<current_state>` tươi cho turn prompt) |
| [apps/desktop/sidecar/src/runtime/prompts.ts](apps/desktop/sidecar/src/runtime/prompts.ts) | Các block system-prompt dùng chung: VERIFY / TOOL_DISCIPLINE / TODO_USAGE / BACKGROUND_EXEC + **ENGINEERING / EVIDENCE / COMMUNICATION** ([ADR 0071](docs/decisions/0071-senior-engineer-prompt-core.md)) |
| [apps/desktop/ui-next/components/skill/](apps/desktop/ui-next/components/skill/) | SkillDetail, SkillEditor, SkillPromptCreator (mini chat), SkillBodyEditModal |
| [apps/desktop/ui-next/components/agent/](apps/desktop/ui-next/components/agent/) | AgentDetail, AgentEditor (MCP picker, source picker, body edit), AgentPromptCreator, AgentBodyEditModal |
| [apps/desktop/ui-next/components/connection/](apps/desktop/ui-next/components/connection/) | Connections/Sources UI (đổi tên từ MCP theo ADR 0025) |
| [docs/reference/claude-sdk-options.md](docs/reference/claude-sdk-options.md) | Toàn bộ 66 `Options` của Claude Agent SDK + cột AWOG đang dùng option nào ở đâu, và đánh giá những option chưa dùng — đối chiếu lại mỗi lần bump SDK |
| [agentflow-prototype.tsx](agentflow-prototype.tsx) | React prototype gốc — dùng tham chiếu khi port |

## Quy tắc làm việc

- **Không thêm dependency mới** khi chưa có ADR/đồng thuận, đặc biệt với UI lib lớn (đã chọn Tailwind + lucide thay vì thư viện component).
- **Không tạo backend service mới** — engine sẽ là Node.js sidecar duy nhất.
- **Không thêm database** trong MVP. Filesystem + JSON/YAML là data layer.
- **Đừng port toàn bộ React prototype 1-1.** Tận dụng Composition API và Pinia store thay vì replicate `useState` ngang hàng.
- **Không lưu API key vào git.** API key thuộc về `settings` store, chỉ ghi xuống workspace cấu hình local của người dùng.
- **Mỗi feature lớn → một tài liệu ở [docs/features/](docs/features/).** Mỗi quyết định kiến trúc lớn → một ADR ở [docs/decisions/](docs/decisions/).

## Trạng thái port (tham chiếu nhanh)

Trạng thái ui-next: Tasks, Projects, Workflows, Agents, Settings, Markdown editor, Theme system — đã port. **Skills** SKILL.md folder format ([ADR 0013](docs/decisions/0013-adopt-skill-md-format.md)) với 2-tier discovery trên `.claude` + chat-driven creation. **Agents** AGENT.md 2-tier ([ADR 0015](docs/decisions/0015-agents-persisted-runtime-systemprompt.md)) với runtime injection systemPrompt + tools + skillIds + mcpServerIds (Pha 2A). **MCP Servers** stdio + http transport ([ADR 0014](docs/decisions/0014-mcp-servers-stdio-runtime.md)) + secret keychain ([ADR 0018](docs/decisions/0018-mcp-secret-keychain.md)) + per-agent whitelist + idle stop (tool call in-process qua `runtime/tools/mcp-tools.ts` dưới Pi SDK). **Context Providers** deprecated, fold vào MCP ([ADR 0016](docs/decisions/0016-deprecate-context-providers-fold-into-mcp.md)). **Filesystem watcher** trong sidecar (chokidar) auto-refresh UI khi file `.md`/`.json` thay đổi ngoài app. **Git Manager** M0..M7 wired ([ADR 0017](docs/decisions/0017-git-manager-ipc-contract.md)). **Tasks + Workflows** wired thật ([ADR 0024](docs/decisions/0024-task-execution-engine-ipc-contract.md)): Workflow Builder persist DAG ra đĩa; Task Execution Engine chạy node qua **Pi SDK** single runtime (parallel scheduler, trace stream, git auto-commit per phase, approve/rerun/discuss, restart-safe resume từ JSONL). Store `tasks`/`workflows` riêng, subscribe `task.*` events ở app-lifetime. **Session ↔ Task link 2 chiều** ([ADR 0055](docs/decisions/0055-session-task-link.md), [spec](docs/features/session-task-link.md)): `TaskSource` thêm biến thể `session` + `Session.aboutTaskId`, chiều ngược derive client-side; spawn task từ chat (tool model `RunWorkflow` gated mutating, depth=1 — nút "Run as task" ở composer đã gỡ 2026-08-19) và "Discuss in session" từ Task (chèn `<linked_task>` context mỗi turn). **Session checklist = shared state** ([ADR 0069](docs/decisions/0069-editable-session-checklist.md), [spec](docs/features/todo-list.md)): `Session.todos` là field authoritative (persist trong `SessionHeader`), `TodoWrite` ghi qua `ToolFilter.todoSink` (chat runtime cấp sink; Tasks giữ ACK), user ghi qua `sessions.updateTodos`, list được inject lại mỗi turn dưới dạng `<session_checklist>` nên sửa của user không bị ghi đè. Banner ghim không tự ẩn (thu thành strip `done/total`), row click cycle trạng thái; step inline trong transcript vẫn read-only (log lịch sử). **⚠ Sửa 2026-09-07 — nhánh Claude SDK KHÔNG còn `TodoWrite`:** CLI 2.1.233+ gỡ tool đó trên opus 4.8 / sonnet 5 / fable 5 và mới hơn, và `CLAUDE_CODE_ENABLE_TODO_TOOLS=1` chỉ trả về nhóm **`TaskCreate`/`TaskGet`/`TaskList`/`TaskUpdate`** (đo trên 2.1.263) — nudge cũ vì thế bảo model gọi một tool không tồn tại (`No such tool available: TodoWrite`) trong khi checklist im lặng. Nay nudge là `TASK_CHECKLIST_PROMPT` (gate theo `TaskCreate`) và `Session.todos` được replay từ CRUD của task tool qua `runtime/claude-sdk/task-checklist.ts` (đọc structured output `SDKUserMessage.tool_use_result`, gộp thành **một** row `todo-list`); `TodoItem.taskId` persist để `TaskUpdate` của turn sau resolve được (seed qua `RunStreamArgs.sessionTodos`), `sessions.updateTodos` tự gắn lại id theo `content` nên user sửa checklist không làm mất id. Nhánh Pi giữ nguyên `TodoWrite`. **Session Workspace Panel** ([spec](docs/features/workspace-panel.md)) — right-docked, 7 tab (Diff/Files/Browser/Plan/Terminal/Tasks/Preview — tab Plan có thêm section Checklist); `fs.*` read-only + `terminal.*` PTY ([ADR 0019](docs/decisions/0019-pty-terminal-in-sidecar.md)). **Tab Browser = trình duyệt của agent, nhúng thật** ([ADR 0086](docs/decisions/0086-embedded-browser-panel.md) revisit window-model của [ADR 0043](docs/decisions/0043-browser-tool-embedded-chromium.md), [spec](docs/features/session-browser-panel.md)): tab đổi từ `BrowserWindow` ẩn sang **`WebContentsView`** nên nhúng được vào panel (nút một-cú-bấm ở status bar), có tab strip + URL bar + back/forward/reload + **ghim trang** + **copy URL** + popout, và **tự theo tab agent đang dùng**. **Ghim ≠ ghim tab** (`useBrowserPins`, localStorage `awog.browserPins`, cap 12): tab thuộc về agent (nó mở/đóng, `browser.close()` xoá sạch lúc thoát) nên pin phải sống lâu hơn mọi tab ⇒ pin là danh sách `{url,title}` persist, bấm chip = mở **tab mới**; so URL bỏ `/` cuối vì `wc.getURL()` chuẩn hoá còn URL gõ tay thì không. Copy lấy `activeTab.url` (URL tải thật), KHÔNG lấy `urlDraft` (chuỗi người dùng đang gõ). **Thanh công cụ xuống dòng thay vì bị bóp:** panel kéo được xuống 240px, ở đó hàng đơn bóp ô URL còn 41px ⇒ chia 2 nhóm cố định (nav 74 / actions 96) + `flex: 1 1 110px` cho ô URL + `flex-wrap` — basis 110 là **số học** (nav+basis+actions+gap+padding ≤ bề rộng panel) nên 322px mặc định vẫn MỘT dòng, dưới ~305px thì 4 nút xuống dòng dưới; đo mọi bề rộng 240–560: không tràn, đủ 7 nút. ⚠ Cửa sổ **< 1040px thì `prototype.css:640` ẩn HẲN mọi workspace panel** (`display:none!important`, có sẵn từ prototype, không riêng Browser) — chip status bar vẫn sáng; view được gỡ đúng nhờ luật `onScreen` nên không để lại lớp phủ chết. Ba chỗ trú của một tab: cửa sổ **holder vô hình** (mặc định) · nhúng trong cửa sổ gọi `attach()` · popout. Hai thứ phải bù, đo được: view chưa từng lên màn hình thì `capturePage` **ném lỗi** ⇒ `ensurePainted` `showInactive()` holder đúng một frame (nhờ vậy `screenshot` vẫn chạy khi không có UI nào mở), và `backgroundThrottling: false` vì timer bị đóng băng trong compositor ẩn. Renderer sở hữu **rect**, main sở hữu **cửa sổ** — `browser:attach|bounds|detach` lấy cửa sổ từ `event.sender` (không window id qua IPC), rect bị `clampRect`. View native **vẽ trên toàn bộ DOM** ⇒ khi app mở bề mặt nổi chồng lên khung thì **trang phải nhường chỗ**, và luật đó đã qua **3 đời**: (1) danh sách class không đo hình học — thiếu sót + ẩn cả trang vì menu ở góc kia màn hình; (2) hit-test `elementFromPoint` trên lưới điểm — bắt đủ nhưng ĐẮT (vài trăm lượt ép layout mỗi mutation) và bắn cả vì toast/chip dock ⇒ **trang nhấp nháy**, người dùng bác thẳng; (3) **nay = danh sách selector `OVERLAYS` + chồng lấn rect ≥24px cả hai trục** (`.ovl.on`/`.smenu`/`.aselmenu`/`.pop`/`.sttpop`/`.lop-scrim`/`.cmdk-ovl`/`.tph-ovl`/`.lbox`/`.shell-scrim`/`.dropzone`) — một `querySelectorAll` + vài `getBoundingClientRect`, **KHÔNG có `.toast`/`.mdock`** nên toast chớp và chip dock ở góc không bao giờ làm trang biến mất; khi ẩn thì khung để TRỐNG (key i18n `sessions.workspace.browser.hidden` đã xoá). ⚠ Cái giá: **danh sách phải bảo trì** — thêm overlay mới mà quên khai báo thì trang đè lên nó (repo không có wrapper modal chung: 23 chỗ tự viết `<div class="ovl" :class="{ on }">`). 12 kịch bản đo trên Chromium thật đều PASS. Trigger = MutationObserver `childList`+`class`+`style` trên `document.body` **đi qua `nudge`** (gộp 1 lượt/frame; đời 2 chạy hit-test thẳng trong callback — chính là cái "giật giật"). Mọi đường gỡ view KHÁC vẫn nguyên: `onScreen` (`isConnected` + hộp ≥ 8px, phủ KeepAlive/`display:none`/panel thu/minimize dock) + `onDeactivated`/`onActivated`. **⚠ `sync()` KHÔNG được `if (syncing) return`** (sửa 2026-09-12) — `syncing` chỉ bật lúc `attach`/`setBounds` đang bay, tức đúng lúc đang cuộn/stream, nên cú `detach` do modal mở bị NUỐT và không ai gọi lại (watcher chỉ bắn khi trạng thái ĐỔI, modal thì không cuộn) ⇒ "nhiều lúc browser vẫn đè lên modal"; nay xếp hàng gộp thành một vòng chạy lại (`syncAgain`) + nhớ rect đã gửi (bỏ IPC `setBounds` trùng mỗi frame cuộn) + không tự giật view về từ popout (chỉ attach khi `!shownElsewhere` hoặc vừa tự nhận `owner`). Popover link neo theo **điểm pointerdown cuối** vì 13 call site mở link bằng lệnh không có MouseEvent, và `el.click()` từ code cho `clientX/Y = 0` ⇒ coi (0,0) là thiếu toạ độ, không phải góc trên trái (⚠ nhưng **dịch thì KHÔNG**: `useSelectionTranslate` có `surface: 'dom'|'browser'` — nguồn `browser` render `BrowserTranslateStrip` trong **chrome** thay vì popover nổi, vì ẩn trang để hiện popover là phá đúng việc người dùng đang làm; `.sttpop` vẫn ở trong OCCLUDING cho selection DOM), và **⚠ khi khung không còn hiện trong document** — app giữ 2 lớp KeepAlive (`<NuxtPage keepalive>` + `<KeepAlive :max=5>` quanh `SessionDetail`) nên đổi trang/đổi session chỉ **deactivate**, `onUnmounted` KHÔNG bắn ⇒ bản đầu để trang agent đè lên UI mới (lỗi thật 2026-09-09). Điều kiện đúng: `el.isConnected` + hộp ≥ 8px (phủ KeepAlive/`display:none`/panel thu/minimize dock) **cộng** `onDeactivated` gỡ ngay + `onActivated` gắn lại; và **chỉ owner hiện tại được detach** vì Vue có thể activate instance mới trước khi deactivate instance cũ mà `detachFrom(window)` park mọi tab của cửa sổ; một lúc chỉ **một dock** hiển thị được (trọng tài cấp module trong `useEmbeddedBrowser`). **Ba lỗi "renderer tưởng mình biết mà không đo" (sửa 2026-09-09):** nút mở rộng panel nhảy thẳng lên `WP_MAX` 560 bất kể cửa sổ ⇒ row 680 thì **chat còn 114px** — nay trần = `max(WP_MIN, min(WP_MAX, 60% hộp, hộp − sàn chat 320))` — **sàn là WP_MIN 240 chứ KHÔNG phải WP_DEFAULT** (sửa 2026-09-12: lấy mặc định làm sàn thì cửa sổ hẹp nâng trần lên đúng bằng mặc định ⇒ `expanded` đúng ngay ở cỡ mặc định, nút khoe icon "thu lại" mà bấm vào không làm gì = "lỗi expand"), cộng `shrinkTarget = min(WP_DEFAULT, trần)` (không kẹp thì cú thu lại trả 322 rồi bị clamp kéo xuống ngay — đá qua đá lại) và `canExpand` **tắt nút** khi hết chỗ; watcher clamp theo **CHỈ `expandTarget`**, không theo `panelSize` (theo cả hai thì mỗi `pointermove` của tay kéo bị kéo ngược về trần trong cùng tick ⇒ panel giật và không kéo rộng quá 60% được), và trần phải là **ref cập nhật bằng ResizeObserver trên hộp flex chứa panel** (đọc DOM trong computed thì không tính lại khi đổi cỡ cửa sổ), cộng watcher **ghi lại cỡ** khi cửa sổ nhỏ lại (panel `flex: 0 0 <size>` không tự co); `browser:attach` **không được ném** vì `tabId` cũ — id từ renderer là cache, attach là yêu cầu HÌNH HỌC nên id lạ rơi về tab active và trả id thật để renderer tự sửa; và đóng popout thì `applyList` phải **nhận lại view** khi thấy `!shown && !shownElsewhere` (nhánh cũ chỉ chạy khi tab active đổi, mà lúc đó nó không đổi). ⚠ Người dùng gõ được trong tab ⇒ **đăng nhập của user rơi vào partition `persist:awog-browser` mà agent đọc được** — cố ý, và là hạng mục đầu của **infosec re-audit còn nợ**. **Phần B — nhập cả profile browser thật** ([spec](docs/features/browser-profile-import.md)): nút ⬇ trong toolbar tab Browser → picker browser (Chrome/Edge/Brave/Arc/Vivaldi/Chromium) → picker profile (tên + email từ `profile.info_cache`) → nhập `Cookies` + `Local Storage` + `IndexedDB`; **KHÔNG bao giờ** `Login Data`/`Web Data`/history/bookmark (cố ý, ghi ở đầu `browser-import.ts`). Cookie `v10`/`v11` giải mã bằng khoá Keychain (PBKDF2 `saltysalt`/1003 → AES-128-CBC, cắt 32 byte tiền tố **chỉ khi** khớp SHA-256(`host_key`)); **`v20`+ = app-bound, không app ngoài nào đọc được** ⇒ tính năng **đo nhãn từng dòng và báo cáo đủ ô** (imported/appBound/keyUnavailable/expired/rejected/undecryptable) chứ không báo thành công chung. Local Storage/IndexedDB **không** mã hoá nên vẫn sang được khi cookie chết, nhưng là store **LevelDB** ⇒ phải **staging** rồi `applyPendingImport()` dời vào partition **lúc boot trước `engine.start()`** (cần khởi động lại; và modal nhắc đóng browser nguồn kẻo copy ra bản torn). Zero dep mới (`sqlite3` CLI + `node:crypto` + `security` CLI). Đường lùi: *Xoá dữ liệu trình duyệt của agent* → `clearStorageData()`. Nhánh `v10` **chưa verify trên dữ liệu thật** (chỉ test bằng jar tự dựng, 4 nhánh). **Phần C — bấm link thì mở ở đâu** ([spec](docs/features/link-open-choice.md)): popover neo tại chỗ bấm (Mở trong AWOG / Mở bằng browser của tôi + "Luôn dùng cách này"), mặc định `ask`, đổi ở **Settings → Workspace → Mở link**; `⌘/Ctrl/Shift-click` = ra ngoài không hỏi. **MỘT listener capture trên `document`** (`useLinkOpen().installInterceptor()`, cài trong `LinkOpenHost` ở `AppGlobalHosts`) phủ **mọi** `<a href^=http>` trong app — KHÔNG sửa call site (opt-out bằng `data-link-raw`; chip path tương đối không khớp nên PreviewModal giữ nguyên); 13 chỗ mở link **bằng lệnh** đổi sang `openLink()`. Handler `will-navigate`/`setWindowOpenHandler` ở `window.ts` **giữ nguyên làm lưới an toàn** (surface không mount host, hoặc script tự set `location.href`). Mở trong app **luôn tab MỚI** (agent có thể đang giữa lượt trên tab active). `setWindowOpenHandler` của trình duyệt agent đổi `deny` → mở tab thật (vẫn `hostBlocked`). **4 chỗ CỐ Ý không hỏi:** 2 dialog OAuth + `source.oauth-url` của connections store (auth code không được vào jar của agent) + link tải Tailscale (partition agent chặn download ⇒ "mở trong app" là ngõ cụt). Link **trong iframe** vẫn bị `will-frame-navigate` chặn thẳng — hàng rào exfiltrate, không phải bỏ sót. Mode ở localStorage (`awog.linkOpenMode`), KHÔNG qua IPC. **Session popout window** ([spec](docs/features/session-popout-window.md)): mở 1 session ra cửa sổ Electron riêng (route `/session?id=<engineId>`, `session-window.ts` keyed theo engineId) theo mô hình **hand-off** — cửa sổ chính hiện placeholder và bỏ qua event của session đó (`ownsSession` gate trong store); `engine:event` fan-out cho cửa sổ chính + popout session (KHÔNG cho tray popover); chặn pop out giữa lượt đang chạy. **Điều hướng transcript + chặn thao tác phá huỷ** ([ADR 0074](docs/decisions/0074-session-message-anchor-and-transcript-navigation.md) neo `eid` cho cả 3 role, [ADR 0075](docs/decisions/0075-transcript-surface-scoping.md) phạm vi theo "surface", [ADR 0076](docs/decisions/0076-compact-is-not-a-bookmark-prune-path.md) `/compact` **KHÔNG** prune bookmark; spec [navigation](docs/features/session-transcript-navigation.md) + [guard](docs/features/session-destructive-action-guard.md)): **đánh dấu (bookmark)** persist trong `SessionHeader` (`Session.bookmarks` + RPC `sessions.updateBookmarks`, cap 30, sort theo thời gian **message** ASC, resolve **chỉ** qua `map.get(b.id)` — không fallback index, tự prune ở **đúng 3 đường cắt** `rewind`/`resend`/`regenerate` + lọc tường minh khi `fork`), hiện ở thanh đầu transcript; **tìm trong phiên** `⌘F` (`useSessionFind` — lớp DATA đếm `n/N` từ `s.msgs`, DOM chỉ để highlight; dùng chung `common/FindBar.vue` với PreviewModal); nhảy tới message đi qua **provide/inject theo surface** (`useTranscriptSurface`) nên 2 tab SSH co-pilot cùng session không giành nhau, **không** `document.querySelector`; **4 hành động cắt transcript** (`rewind`/`resend`/`edit & resend`/`regenerate`) phải qua `await confirm()` nêu đúng số message sẽ mất (**chỉ** hỏi khi `lostCount ≥ 1`), 5 hành động tô `danger` khi hover, và mọi lần cắt trên đĩa neo vào `lastPersistedEid` — **không** theo `role` (`null` = xoá sạch file); `fork` không persist banner `ENGINE_UNAVAILABLE`. System tray cơ bản đã wire (Electron main); native notification dùng Notification API của renderer (Chromium). **LLM runtime** ([ADR 0029](docs/decisions/0029-migrate-llm-runtime-to-pi-sdk.md)): Pi SDK single multi-provider runtime (Anthropic/OpenAI/Google/custom) thay `@anthropic-ai/claude-agent-sdk`; Sessions + Tasks + 7 one-shot method dùng `runtime/tools/mcp-tools.ts` (in-process). **⚠ Sửa 2026-08-27 — KHÔNG còn "single runtime":** runtime chọn **theo provider** ([ADR 0058](docs/decisions/0058-claude-agent-sdk-vs-pi-runtime-revisit.md) revisit phần single-runtime của 0029) — `provider === 'anthropic'` ⇒ `runtime/claude-sdk/run-stream.ts` (`@anthropic-ai/claude-agent-sdk`, vẫn là dependency), provider khác ⇒ `runtime/run-stream.ts` (Pi); `/compact` **luôn** qua Pi. Xem `sessions/runner.ts` + `runtime/invoke.ts:392`. **⚠ Sửa 2026-09-12 — runtime THỨ BA:** `provider === 'openai'` (trừ account có `baseURL`) ⇒ `runtime/codex/run-stream.ts` — daemon `codex app-server` ([ADR 0087](docs/decisions/0087-codex-app-server-as-openai-runtime.md), [spec](docs/features/codex-runtime.md)), thay hẳn Pi chứ KHÔNG opt-in (user chốt, ngược đề xuất của ADR). Một daemon cho mỗi account (`CODEX_HOME` riêng ở `~/.awog/codex/<accountId>` — mượn `~/.codex` của user là **nạp nhầm MCP + model của họ**), một thread cho mỗi phiên (`Session.codexThreadId`). Tool AWOG bắc cầu in-process qua `dynamicTools`, TRỪ `Read/Write/Edit/MultiEdit/Bash/BashOutput/KillShell/Grep/Glob` — để cho harness của Codex (đó là lý do sang đây); duyệt shell/file của nó về qua `ServerRequest` và AWOG hỏi cổng quyền dưới tên `Bash`/`Edit` nên luật `Bash(git status)` cũ **vẫn khớp**. **3 bẫy đo được:** (1) `mcp__` là tiền tố DÀNH RIÊNG — gửi nguyên tên thì `thread/start` hỏng NGUYÊN LƯỢT ⇒ đổi trên dây thành `awogmcp__…` rồi trả lại tên gốc trước cổng quyền + step mapper; (2) content item là `inputText` chứ không phải `text`, sai shape thì server KHÔNG ném lỗi mà đưa chuỗi `dynamic tool response was invalid` cho model làm kết quả tool; (3) `thread/resume` KHÔNG nhận `dynamicTools` (bộ tool ràng vào thread lúc start) ⇒ `Session.codexToolSignature` lưu chữ ký, lệch thì mở thread MỚI, nếu không MCP đính kèm giữa phiên im lặng không tồn tại. `/compact` + Tasks + ~20 one-shot + account có `baseURL` (gateway tương thích-OpenAI) vẫn ở Pi. Bộ tool của một lượt chat dựng ở `runtime/chat-toolset.ts` dùng CHUNG cho Pi + Codex. ADR 0058 và [dual-sdk-runtime.md](docs/features/dual-sdk-runtime.md) còn ghi *"chưa bắt đầu code"* ⇒ **tech-lead cần xác định P0–P5 ship tới đâu** rồi viết lại đoạn này cho gọn. Pha 2A completion: [docs/features/phase-2a.tasks.md](docs/features/phase-2a.tasks.md). **Mobile Remote Control** ([ADR 0067](docs/decisions/0067-mobile-remote-control-transport.md), [spec](docs/features/mobile-remote-control.md)): Remote Gateway ở Electron main (`remote-gateway*.ts`) bind **tailnet-only, fail-closed**, opt-in mặc định TẮT ở Settings→Devices, QR pairing port 47600; PWA riêng [apps/desktop/remote-pwa/](apps/desktop/remote-pwa/) (Vite+Vue, không Nuxt). P1 = view/send/approve; **P2 (2026-08-09)** = steer/cancel/checklist/session-create + step detail + đính kèm ảnh + search + service worker. Mọi lần **mở rộng allowlist ⇒ infosec re-audit bắt buộc**; policy tập trung ở [remote-gateway-policy.ts](apps/desktop/electron/src/remote-gateway-policy.ts) (allowlist exact-match + param-pick default-deny) và [remote-gateway-catalog.ts](apps/desktop/electron/src/remote-gateway-catalog.ts) (method local `remote.bootstrap`, result-pick). **GitHub notifications** ([spec](docs/features/github-notifications.md)): poll `gh.notifications` (account-scoped, không repo cwd) → **hộp thông báo bell ở TopBar** (`TopBarNotifications` + `useGhInbox`, badge chưa đọc, tab Tất cả/Đang theo dõi, mark-read từng thread hoặc cả hộp qua `gh.notificationsRead`) + toast bấm được + native notification khi mất focus; **opt-in theo project chỉ gate việc BÁO, không gate việc hiện** (bell luôn đầy đủ, rỗng ⇒ hộp thư thật sự sạch); kênh gửi + vị trí toast + công tắc từng nguồn gom ở **Settings→Thông báo** (`settings.notifications`: delivery toast/native/both, toastPosition, sessionEvents), dedupe theo `(id, updatedAt)`, seed im lặng chỉ 1 lần/máy (mốc = `polledAt`), floor 60s. **Wiki** ([ADR 0073](docs/decisions/0073-wiki-as-llm-context-source.md), [spec](docs/features/wiki.md)): trang tài liệu trong app (`/wiki`) **đồng thời là nguồn context cho LLM** — store 2 tier `~/.awog/wiki` + `{project}/.awog/wiki` (space = folder cấp 1, trang = `.md`, `[[wikilink]]` + backlink), import `.md` bằng dialog/kéo-thả (copy-in, allowlist đuôi + cap 1MB/trang), 9 RPC `wiki.*`, watcher `wiki.fs-changed`. Prompt mỗi turn chỉ nhận **mục lục** `<wiki_index>` (cap 4k ký tự, vượt thì giảm cấp xuống space và nói rõ); nội dung đọc theo yêu cầu qua tool `wiki_search`/`wiki_read` — wire **cả 2 runtime** (Pi = AgentTool, Claude SDK = `mcp__awogwiki__*`) vì `Read` bị `assertInsideWorkspace` chặn ở nhánh Pi. Trang `context: false` = ghi chú riêng, LLM không thấy. UI: `/wiki` (cây resize được + reader + Monaco editor + context menu) và **Settings → Wiki** (công tắc nạp + ngân sách). **Bộ nhớ AI** (phần B, [spec](docs/features/ai-memory.md)): 1 fact = 1 file `.md` 2 tier (`~/.awog/memory`, `{project}/.awog/memory`, frontmatter `name`/`description`/`type`/`enabled`, KHÔNG có `MEMORY.md` — index derive từ frontmatter), nạp `<memory>` mỗi turn nhóm theo `type`; tool `memory_remember`/`memory_forget`/`memory_read` (2 runtime), **agent tự ghi opt-in mặc định TẮT** ở Settings → Bộ nhớ, cờ đi qua IPC per-turn (`contextConfig`, KHÔNG để sidecar đọc settings.json); Tasks chỉ ĐỌC memory, không ghi. **Giám sát tài nguyên** ([ADR 0090](docs/decisions/0090-activity-monitor-process-attribution.md), [spec](docs/features/activity-monitor.md)): trang `/monitor` đo CPU/RAM theo tiến trình + gộp **theo phiên**, **GPU đo được THEO TIẾN TRÌNH** (⚠ đính chính bản đầu vốn ghi "đòi root": chỉ `powermetrics` cần root, IORegistry thì không — `ioreg -r -c IOAccelerator -l -w 0` trả 70 client kèm pid trong 17ms/67KB): `AppUsage.accumulatedGPUTime` (ns) + `IOUserClientCreator`="pid N" ⇒ cùng phép hiệu 2 nhịp như CPU; gom theo KHỐI `+-o …UserClient` vì một pid có nhiều client. Thẻ GPU nói "GPU · AWOG", bảng có cột GPU sort được, cột ẩn theo "có tiến trình nào đo được" chứ KHÔNG theo `process.platform` (máy đo có thể ở đầu kia SSH). **Khối "Máy" riêng** (`MonitorSystem.vue`): từng lõi + load average qua `os.cpus()`/`os.loadavg()` (không native module, không root — `kern.cp_times` KHÔNG tồn tại trên macOS, `top` không in per-core), và **RAM thật + swap** — đây là SỬA SAI về tính đúng đắn: tổng RSS KHÔNG phải RAM đang dùng (đo thật: RSS 9.9GB trong khi máy 16GB dùng 15.9GB + swap 10.7GB). ⚠ **ĐÍNH CHÍNH LẦN HAI**: bản sửa đầu dùng `used = total − (free + speculative)` vì khớp `top` từng MiB — khớp nhưng VÔ DỤNG, nó tính cả `inactive` (file cache) nên ra **99% trên mọi máy Mac chạy vài tiếng**, vạch lúc nào cũng đỏ (user bắt: "số đỏ này hơi ảo không"). Nay dùng **`App (anonymous − purgeable) + Wired + nén`** = đúng "Memory Used" của Activity Monitor (đo: 13.2/16 GB = 83%), file cache hiện thành **đoạn nhạt riêng**, và **màu lấy từ `kern.memorystatus_vm_pressure_level`** (1/2/4) chứ KHÔNG từ phần trăm — phần trăm là chỉ báo tồi vì macOS cố tình giữ RAM đầy. **Swap chỉ hiện SỐ TUYỆT ĐỐI**: `vm.swapusage.total` là kích thước swapfile hiện tại mà macOS TỰ NỚI, không phải trần — hiện "11.4/12.0 GB = 95%" là bịa ra cái trần rồi báo động vì sắp chạm nó; màu so với RAM vật lý (20/50). Linux `/proc/meminfo` (`MemTotal − MemAvailable` vốn đã trừ cache). Nhiệt độ/điện năng cần IOReport native ⇒ KHÔNG làm. Nguồn là `ps` (zero dep), CPU là **hiệu giữa 2 nhịp** chứ không phải cột `%CPU` (cột đó là trung bình từ lúc sinh ⇒ tiến trình vừa ngốn 100% trong 3s mà đã sống 10h vẫn hiện ~0%) ⇒ `monitor.sample` CÓ TRẠNG THÁI và UI phải poll. Chỉ nhánh **Claude SDK** có tiến trình riêng cho mỗi phiên (argv mang `--resume=<sdkSessionId>` ⇒ map là TRA BẢNG); nhánh **Pi** chạy in-process trong sidecar và nhánh **Codex** dùng chung daemon theo account ⇒ hai nhánh đó hiện "—" + câu giải thích, KHÔNG hiện số của tiến trình dùng chung như thể là của phiên. Dò được **tiến trình AWOG mồ côi** (mang dấu bản cài nhưng ngoài cây instance hiện tại — ca thật lúc khảo sát: sidecar mồ côi 17h25m ở 99,7% CPU). Dừng được tiến trình, nhưng **sidecar cưỡng chế** chứ không tin pid từ UI (pid tái sử dụng + ảnh chụp cũ 2s), cấm giết hạ tầng của chính instance. **Ba phạm vi đo** (`monitor.sample` nhận `scope`): `awog` (cây của app, mặc định) · `machine` (cả máy — user ĐẢO chốt ban đầu: "mỗi tiến trình AWOG thì không có nhiều value lắm") · `ssh` + `connId` (máy ở đầu kia một kết nối ĐANG MỞ — cố ý KHÔNG tự `connect`). Bản chụp trước để tính hiệu CPU **key theo nguồn** (`local`/`ssh:<connId>`) vì pid hai máy trùng nhau như chơi. Đo cả máy thì danh sách bị CẮT (hợp của top-150 CPU + top-60 RAM + toàn bộ cây AWOG) nhưng `totals` vẫn cộng trên tất cả. Nhãn thẻ đi theo phạm vi (`CPU · Toàn máy`, `GPU · prod-web-01`) — con số cả máy mà nhãn ghi "của AWOG" là đúng thứ trang này tồn tại để tránh. **Kill đã NỚI**: cấm hạ tầng của chính instance + `pid ≤ 1`, còn lại cho phép KỂ CẢ tiến trình ngoài AWOG — ⚠ luật bảo vệ ở UI phải gate theo `MonitorProcess.own` (thuộc cây instance đang chạy) chứ KHÔNG theo `kind`: bản đầu gate theo kind nên ở phạm vi toàn máy mọi tiến trình Chromium của Chrome/Claude VÀ sidecar AWOG mồ côi đều mất nút dừng (sidecar vốn đã kiểm đúng `inOurTree && PROTECTED`, thiếu sót nằm ở UI). **Gom theo ứng dụng** (công tắc ở đầu bảng): một hàng/app với tổng CPU/GPU/RAM, mở ra thấy từng tiến trình, + nút dừng CẢ app (gửi tín hiệu từng tiến trình, KHÔNG đoán "tiến trình chính" rồi trông chờ nó kéo con theo). Tên app = bundle `.app` NGOÀI CÙNG trên path; ⚠ KHÔNG tách argv0 bằng khoảng trắng vì path bundle macOS có khoảng trắng (`split(/\s+/)[0]` ra `/Applications/Google` → nhãn "Google" cho mọi thứ — lỗi đã đo) (để hệ điều hành phán, `EPERM` đẩy thẳng lên UI); máy từ xa đi qua `kill -TERM|-KILL <pid>` với pid nội suy dạng SỐ. **Thang màu xanh/vàng/đỏ suy từ DUNG LƯỢNG THẬT** (`coreCount` + `totalMemKb` trong snapshot; máy từ xa hỏi `nproc`/`sysctl`+`/proc/meminfo`/`hw.memsize` một lệnh, cache theo kết nối): CPU/tiến trình chia cho MỘT lõi (50/90 — mốc đỏ 90 TRÙNG ngưỡng cảnh báo để màu và thông báo không nói hai chuyện), CPU tổng chia cho lõi×100 (30/70), RAM chia cho RAM vật lý (2/8 per-process, 25/50 tổng), GPU 50/85. ⚠ Dùng token `--green`/`--amber`/`--danger`, KHÔNG dùng `--accent` cho mức "khoẻ" — accent người dùng đổi được, chọn accent đỏ là thang mất nghĩa; `totalMemKb = 0` ⇒ không tô màu RAM thay vì tô theo mẫu số bịa. Thẻ có vạch mức, số cột theo **container query** (2 hoặc 4, KHÔNG BAO GIỜ 3). Hai khối dưới là card `.tile`; bảng nhúng trong banner mồ côi khử skin bằng `:deep`. **Sort ở tiêu đề cột** (bấm lại đảo chiều, caret ▲/▼) — cụm nút CPU/RAM/PID rời đã bỏ. Ô tên: dòng 1 tên, dòng 2 tag + lệnh; `table-layout: fixed` KHÔNG tự cắt nội dung tràn nên ô phải `overflow: hidden` (thiếu thì hàng tag đè lên cột PID/CPU — lỗi đã đo). **Tab "Đĩa"** (`monitor/disk.ts` + `MonitorDisk.vue`): ổ đĩa (chỉ ổ chứa HOME + ổ ngoài — `df` đầy đủ in 7 dòng cùng số vì APFS dùng chung container; lọc thêm volume cùng container và DMG < 1GB), danh mục rác cố định nhãn `safe`/`review` kèm câu "mất gì khi xoá", + build dir cấp 1 của project (UI truyền `projectRoots`, sidecar KHÔNG tự chọn cây để quét), + **quét sâu = DRAWER** (`MonitorDiskDrawer.vue`) có TIẾN ĐỘ THẬT — ⚠ `du` KHÔNG stream: bản đầu đọc stdout của `du -kx -d 1` theo dòng, tin rằng nó in từng mục con khi xong; đo được là trên macOS stdout-là-pipe thì libc đệm KHỐI nên 400 dòng về cùng lúc ở giây 21.7 (streaming trên giấy, spinner thực tế). Nay: `readdir` cha (tức thì ⇒ biết `n/tổng`) → `stat` cho file → `du -skx` TỪNG thư mục, 4 luồng song song, phát `disk.tree-list`/`tree-entry`/`tree-done` lọc theo `scanId`, huỷ được khi đóng drawer/đổi thư mục (đo lại: danh sách 1ms, kết quả đầu 7ms, xong 11.7s — nhanh HƠN bản cũ 21.7s); `TreeEntry.isDir` từ sidecar ⇒ thư mục = `<button>` + icon accent, file = `<div>` + icon xám (⚠ bản đầu mọi dòng đều là button nên bấm vào file là `readdir` trên file → lỗi), breadcrumb `~/Library/Caches` bấm từng cấp để nhảy về chứ KHÔNG phải khối ở đáy trang — nó là luồng lần xuống nhiều cấp, để ở đáy thì mỗi cú bấm phải cuộn đi cuộn lại (user bác bản đầu); vạch tỉ lệ so với mục lớn nhất cùng cấp, nút lên-một-cấp tự tắt ở HOME, Esc/nền đóng, path rút về `~/…` (⚠ KHÔNG dùng `direction: rtl` để cắt đầu chuỗi — nó đảo dấu `/` đầu xuống cuối). ⚠ `du` CHẬM (đo: 19.5s cho `~/Library/Caches` 19.7GB) ⇒ API tách đôi `disk.targets` (chỉ `stat`, trả ngay) + `disk.size` TỪNG mục, UI chạy 3 song song và hiện dần; đo là cú bấm có chủ ý, KHÔNG tự chạy khi mở tab. Tổng "dọn an toàn" chỉ cộng mục `safe` (đo thật trên máy dev: 33.5 GB). **Xoá = `shell.trashItem` của Electron (HOÀN TÁC ĐƯỢC), KHÔNG `rm -rf`** — ba hàng rào cưỡng chế ở main chứ không tin UI: tuyệt đối + `realpath` (khử symlink) · trong HOME nhưng KHÔNG phải chính HOME · sâu ≥ 2 cấp. ⚠ **ĐỌC và XOÁ có phạm vi KHÁC NHAU, cố ý**: đọc phủ CẢ ĐĨA (`assertScannable` chỉ chặn filesystem ảo `/dev`,`/proc`,`/sys`,snapshot TM) — bản đầu khoá trong HOME nhưng thế là chặn luôn câu hỏi chính "360GB nằm ở đâu"; xoá vẫn chỉ trong HOME sâu ≥2 cấp (Electron main). UI ẩn nút Thùng rác ngoài phạm vi xoá. ⚠ **Chuẩn hoá firmlink APFS** (`canonicalPath` trong `assertScannable`): cùng một thư mục có HAI đường dẫn (`/Users/kyro` và `/System/Volumes/Data/Users/kyro`), duyệt từ thẻ ổ sinh ra dạng thứ hai ⇒ MọI phép so khớp tiền tố trượt: mất nút xoá + breadcrumb thành một mẩu chết + nút quay lại vô dụng — user báo 3 lỗi rời rạc nhưng là MỘT. Chuẩn hoá ở BIÊN thay vì bắt mọi nơi nhớ hai dạng; breadcrumb dựng từ SEGMENT cho mọi đường dẫn (`~` chỉ là rút gọn phần đầu); `disk.tree` trả `{scanId, path}` và UI PHẢI dùng `path` trả về chứ không phải cái nó gửi. ⚠ **Drawer/overlay phủ lên thanh trên PHẢI khai `-webkit-app-region: no-drag`**: `.top` là vùng kéo cửa sổ, vùng kéo tính ở tầng cửa sổ nên lớp phủ không tự trừ mình ra sẽ bị NUỐT CLICK (lỗi đã đo khi drawer đổi sang `top: 0`). ⚠ Lối vào "Toàn ổ" trỏ **mount của ổ chứa HOME**, KHÔNG phải `/`: trên APFS `/` là volume hệ thống niêm phong và `du -x` dừng ở biên fs, nên quét `/` ra 32GB Applications mà KHÔNG có `/Users` (đo được; `/System/Volumes/Data` mới ra Users 300GB). **Cảnh báo chủ động** (`composables/useResourceAlerts.ts`, vòng 60s ở renderer, MẶC ĐỊNH BẬT — khác `cicdEvents`): tiến trình mồ côi · CPU cao kéo dài (90% / 5 phút) · RAM vượt ngưỡng (mặc định TẮT vì ngưỡng đúng phụ thuộc máy). Phần khó là CHỐNG SPAM chứ không phải ngưỡng: khoá sự cố theo pid chỉ gỡ khi điều kiện thôi đúng, CPU đo bằng MỐC THỜI GIAN (không đếm nhịp), RAM có trễ 10% khi hạ, khoá chỉ nằm trong RAM (khởi động lại là lúc MUỐN báo lại), `cpuPercent === null` KHÔNG tính là mát, và luật CPU bỏ qua tiến trình mồ côi vì chúng đã có cảnh báo riêng. Kênh gửi tách thành `composables/useAppNotify.ts` (`presentNotification`) — trước đó bị chép nguyên văn ở `useGhNotifications` + `useCicdNotify`, đây là nơi thứ BA nên cả hai nơi cũ đã chuyển sang dùng chung (giữ `nativeTitle` vì hộp thư GitHub đặt TÊN REPO làm tiêu đề thông báo OS). ⚠ Vòng đo ở renderer ⇒ app thu vào tray không còn cửa sổ thì không ai đo. ⚠ Nhận diện "của AWOG" so khớp trên FILE CHẠY chứ không trên cả dòng lệnh — bản đầu dùng `includes` khiến `zsh -c "cd …/sidecar && …"` bị gắn nhãn mồ côi và UI mời giết nó. **Theme "Cute"** ([ADR 0072](docs/decisions/0072-cute-theme-family.md), [spec](docs/features/theme-cute.md)): theme family thứ 2 mint/off-white bên cạnh `awog` mặc định, opt-in ở Settings→Appearance→Theme→Cute (mặc định TẮT), toàn bộ style nằm trong `assets/css/theme-cute.css` scoped theo `body[data-theme-family='cute']` — không phá vỡ giao diện AWOG khi tắt.

## Khi user yêu cầu feature mới

1. Kiểm tra [docs/requirements/mvp-scope.md](docs/requirements/mvp-scope.md) — có trong MVP không?
2. Kiểm tra [docs/features/](docs/features/) — đã có spec chưa?
3. Nếu chưa, đề xuất tạo spec ngắn trước khi code.
4. Code theo [docs/coding/general.md](docs/coding/general.md) + [docs/coding/nuxt-frontend.md](docs/coding/nuxt-frontend.md), cập nhật store/types khi cần.
5. Cập nhật [docs/architecture/system-overview.md](docs/architecture/system-overview.md) nếu thêm route/component mới đáng kể.
